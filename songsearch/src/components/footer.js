import { motion } from "framer-motion";
import { Flags, LangIcon } from "./img/svg";
import { ThemeToggle } from "./dark";

export default function Footer({}) {
    return (
        <div className="fixed inset-x-0 bottom-0">Please accept cookies</div>
    );
}
